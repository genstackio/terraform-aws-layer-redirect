# AWS Redirect Terraform module

Terraform module which creates the resources to host a redirect domain on AWS.

## Usage

```hcl
module "main" {
  source      = "genstackio/layer-redirect/aws"
  
  // ...
}
```
